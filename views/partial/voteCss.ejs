<style>
    :root {
        --main-color: #89a3c5;
    }
    * {
        padding: 0px;
        margin: 0px;
        box-sizing: border-box;
    }
    .large-box {
        height: 90vh;
        display: flex;
        justify-content: center;
        align-items: center;
    }
    .smallbox {
        height: 500px;
        width: 500px;
        margin-inline: 1em;
        padding: 1em;
        display: flex;
        justify-content: center;
        flex-direction: column;
        align-items: center;
    }
    .img {
        display: flex;
        height: 400px;
        width: 450px;
        overflow: hidden;
        position: relative;
    }
    .img img{
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%,-50%);
        width: 300px;
    }
    .button {
        margin: 1em;
    }
    .vote-laptop {
        padding-inline: 2em;
        padding-block: .5em;
        font-size: 22px;
        font-weight: 700;
        background: linear-gradient(to right, #54ABC5, #1337C3);
        border-radius: 20px;
        color: white;
    }
    #show {
        display: none;
    }
    #name {
        color: white;
        font-size: 1em;
        font-weight: 700;
    }

    /*login & Sign in*/
.get-started {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    background: linear-gradient(to right, #54ABC5, #1337C3);
}
.modal-dialog {
    min-width: 100px;
}
form.form-control,.modal-header,.modal-body{
    background: var(--main-color);
    border: none;    
}
.modal-header {
    border: none;
}
.tab-pane{
    background: var(--main-color);
}
.form-control,.checkMark {
    margin-top: 1em;
}
.justify-content-center {
    background:  var(--main-color);
}
a.nav-link {
    color: #706666;
    background: none;
    font-weight: 600; 
}
a.nav-link.active {
    border-bottom: 2px solid;
    color: #fff;
    background: none;
    font-weight: 600;
}
nav.d-flex.justify-content-center {
    margin-bottom: -1rem;
}
.sign {
    border: 2px solid #212529;
    border-radius: 15px;
}
.modal-footer {
    font-size: 15px;
    border: none;
}
.modal-footer a {
    text-decoration: none;
}
.fw-bold {
    font-size: 13px;
}
.card {
    padding: 0px !important;
	background: linear-gradient(to right, #54ABC5, #1337C3);
	cursor: pointer;
	margin: 2px;
}
a.nav-link.active {
    color: rgb(54, 45, 45);
}
.modal-body {
    border: none;
    border-radius: 0 0 10px 10px ;
}
#registration {
    font-size: 1em;
    color: white;
    font-weight: 700;
}
    


</style>